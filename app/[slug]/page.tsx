import type{Metadata}from"next";import{notFound}from"next/navigation";import{InnerPage}from"@/components/inner-pages";const pages=["about","services","work","case-studies","process","pricing","contact"];export function generateStaticParams(){return pages.map(slug=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;if(!pages.includes(slug))return{};return{title:slug.split("-").map(x=>x[0].toUpperCase()+x.slice(1)).join(" ")}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;if(!pages.includes(slug))notFound();return <InnerPage type={slug}/>}
